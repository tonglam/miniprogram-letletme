<view class="teamPicker">

  <van-popup show="{{show}}" overlay="{{true}}" position="top" custom-style="height: 50%" bind:close="onClose">
    <view class="picker">
      <van-picker show-toolbar toolbar-position="bottom" item-height="30" title="" columns="{{columns}}"
        bind:change="onChange" bind:confirm="onConfirm" bind:cancel="onCancel" />
    </view>
  </van-popup>
  
</view>